<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inet.code.mapper.LoginMapper">
        <select id="selectByid" resultType="com.inet.code.entity.po.Login" parameterType="java.lang.Integer">
            select * from login where id=#id}
        </select>
</mapper>
